"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.HeartbeatCheckCodegen = void 0;
const codegen_1 = require("./internal/codegen");
const sourcegen_1 = require("../sourcegen");
const check_codegen_1 = require("./check-codegen");
class HeartbeatCheckCodegen extends codegen_1.Codegen {
    describe(resource) {
        return `Heartbeat: ${resource.name}`;
    }
    gencode(logicalId, resource, context) {
        const filePath = context.filePath('resources/heartbeats', resource.name, {
            tags: resource.tags,
            unique: true,
        });
        const file = this.program.generatedConstructFile(filePath.fullPath);
        file.namedImport('HeartbeatCheck', 'checkly/constructs');
        file.section((0, sourcegen_1.expr)((0, sourcegen_1.ident)('HeartbeatCheck'), builder => {
            builder.new(builder => {
                builder.string(logicalId);
                builder.object(builder => {
                    builder.number('period', resource.heartbeat.period);
                    builder.string('periodUnit', resource.heartbeat.periodUnit);
                    builder.number('grace', resource.heartbeat.grace);
                    builder.string('graceUnit', resource.heartbeat.graceUnit);
                    (0, check_codegen_1.buildCheckProps)(this.program, file, builder, resource, context);
                });
            });
        }));
    }
}
exports.HeartbeatCheckCodegen = HeartbeatCheckCodegen;
//# sourceMappingURL=heartbeat-check-codegen.js.map